SECRET_KEY=django-insecure-change-me-in-production
DEBUG=True
ENABLE_SILK=False

# Comma-separated. Required when DEBUG=False.
ALLOWED_HOSTS=localhost,127.0.0.1

# Comma-separated list of origins allowed to call the API (e.g. your frontend URL).
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Database — uncomment one of the following:

# PostgreSQL (production)
# DATABASE_URL=postgres://user:password@host:5432/dbname

# SQLite (development) — path is relative to repo root
# DATABASE_URL=sqlite:///dev_db.sqlite3
# Note: when DEBUG=True, SQLite is used automatically without setting DATABASE_URL